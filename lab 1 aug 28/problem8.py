city = input("Enter a city: ") #takes city as input from the user as a string
state = input("Enter a state: ") #takes state as input from the user as a string
print(f"You are currently in {city}, {state}.") #outputs a message in the format of "You are currently in city, state."